# Property Investment Dashboard

Upload `index.html` to a GitHub repository and enable **Settings → Pages → Deploy from branch → main → root**.

Your site will then be available at:
`https://YOUR-GITHUB-USERNAME.github.io/REPOSITORY-NAME/`

The dashboard is a static HTML page and uses Chart.js from jsDelivr for charts.
